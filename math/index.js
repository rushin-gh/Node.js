const { printSum } = require("./sum");
const { printSubstraction } = require("./subtract");
const { printMultiplication } = require("./multiply");
const { printDiv } = require("./div");

module.exports = {
  printSum,
  printSubstraction,
  printMultiplication,
  printDiv,
};
